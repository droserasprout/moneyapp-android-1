package com.cactusteam.money.data.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.cactusteam.money.data.dao.TransactionPattern;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TRANSACTION_PATTERN".
*/
public class TransactionPatternDao extends AbstractDao<TransactionPattern, Long> {

    public static final String TABLENAME = "TRANSACTION_PATTERN";

    /**
     * Properties of entity TransactionPattern.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Name = new Property(1, String.class, "name", false, "NAME");
        public final static Property Type = new Property(2, int.class, "type", false, "TYPE");
        public final static Property Comment = new Property(3, String.class, "comment", false, "COMMENT");
        public final static Property GlobalId = new Property(4, Long.class, "globalId", false, "GLOBAL_ID");
        public final static Property Synced = new Property(5, Boolean.class, "synced", false, "SYNCED");
        public final static Property SourceAccountId = new Property(6, long.class, "sourceAccountId", false, "SOURCE_ACCOUNT_ID");
        public final static Property Amount = new Property(7, double.class, "amount", false, "AMOUNT");
        public final static Property CategoryId = new Property(8, Long.class, "categoryId", false, "CATEGORY_ID");
        public final static Property SubcategoryId = new Property(9, Long.class, "subcategoryId", false, "SUBCATEGORY_ID");
        public final static Property DestAccountId = new Property(10, Long.class, "destAccountId", false, "DEST_ACCOUNT_ID");
        public final static Property DestAmount = new Property(11, Double.class, "destAmount", false, "DEST_AMOUNT");
    };

    private DaoSession daoSession;


    public TransactionPatternDao(DaoConfig config) {
        super(config);
    }
    
    public TransactionPatternDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TRANSACTION_PATTERN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"NAME\" TEXT," + // 1: name
                "\"TYPE\" INTEGER NOT NULL ," + // 2: type
                "\"COMMENT\" TEXT," + // 3: comment
                "\"GLOBAL_ID\" INTEGER," + // 4: globalId
                "\"SYNCED\" INTEGER," + // 5: synced
                "\"SOURCE_ACCOUNT_ID\" INTEGER NOT NULL ," + // 6: sourceAccountId
                "\"AMOUNT\" REAL NOT NULL ," + // 7: amount
                "\"CATEGORY_ID\" INTEGER," + // 8: categoryId
                "\"SUBCATEGORY_ID\" INTEGER," + // 9: subcategoryId
                "\"DEST_ACCOUNT_ID\" INTEGER," + // 10: destAccountId
                "\"DEST_AMOUNT\" REAL);"); // 11: destAmount
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TRANSACTION_PATTERN\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, TransactionPattern entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
        stmt.bindLong(3, entity.getType());
 
        String comment = entity.getComment();
        if (comment != null) {
            stmt.bindString(4, comment);
        }
 
        Long globalId = entity.getGlobalId();
        if (globalId != null) {
            stmt.bindLong(5, globalId);
        }
 
        Boolean synced = entity.getSynced();
        if (synced != null) {
            stmt.bindLong(6, synced ? 1L: 0L);
        }
        stmt.bindLong(7, entity.getSourceAccountId());
        stmt.bindDouble(8, entity.getAmount());
 
        Long categoryId = entity.getCategoryId();
        if (categoryId != null) {
            stmt.bindLong(9, categoryId);
        }
 
        Long subcategoryId = entity.getSubcategoryId();
        if (subcategoryId != null) {
            stmt.bindLong(10, subcategoryId);
        }
 
        Long destAccountId = entity.getDestAccountId();
        if (destAccountId != null) {
            stmt.bindLong(11, destAccountId);
        }
 
        Double destAmount = entity.getDestAmount();
        if (destAmount != null) {
            stmt.bindDouble(12, destAmount);
        }
    }

    @Override
    protected void attachEntity(TransactionPattern entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public TransactionPattern readEntity(Cursor cursor, int offset) {
        TransactionPattern entity = new TransactionPattern( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // name
            cursor.getInt(offset + 2), // type
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // comment
            cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4), // globalId
            cursor.isNull(offset + 5) ? null : cursor.getShort(offset + 5) != 0, // synced
            cursor.getLong(offset + 6), // sourceAccountId
            cursor.getDouble(offset + 7), // amount
            cursor.isNull(offset + 8) ? null : cursor.getLong(offset + 8), // categoryId
            cursor.isNull(offset + 9) ? null : cursor.getLong(offset + 9), // subcategoryId
            cursor.isNull(offset + 10) ? null : cursor.getLong(offset + 10), // destAccountId
            cursor.isNull(offset + 11) ? null : cursor.getDouble(offset + 11) // destAmount
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, TransactionPattern entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setType(cursor.getInt(offset + 2));
        entity.setComment(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setGlobalId(cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4));
        entity.setSynced(cursor.isNull(offset + 5) ? null : cursor.getShort(offset + 5) != 0);
        entity.setSourceAccountId(cursor.getLong(offset + 6));
        entity.setAmount(cursor.getDouble(offset + 7));
        entity.setCategoryId(cursor.isNull(offset + 8) ? null : cursor.getLong(offset + 8));
        entity.setSubcategoryId(cursor.isNull(offset + 9) ? null : cursor.getLong(offset + 9));
        entity.setDestAccountId(cursor.isNull(offset + 10) ? null : cursor.getLong(offset + 10));
        entity.setDestAmount(cursor.isNull(offset + 11) ? null : cursor.getDouble(offset + 11));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(TransactionPattern entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(TransactionPattern entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getAccountDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getCategoryDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T2", daoSession.getSubcategoryDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T3", daoSession.getAccountDao().getAllColumns());
            builder.append(" FROM TRANSACTION_PATTERN T");
            builder.append(" LEFT JOIN ACCOUNT T0 ON T.\"SOURCE_ACCOUNT_ID\"=T0.\"_id\"");
            builder.append(" LEFT JOIN CATEGORY T1 ON T.\"CATEGORY_ID\"=T1.\"_id\"");
            builder.append(" LEFT JOIN SUBCATEGORY T2 ON T.\"SUBCATEGORY_ID\"=T2.\"_id\"");
            builder.append(" LEFT JOIN ACCOUNT T3 ON T.\"DEST_ACCOUNT_ID\"=T3.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected TransactionPattern loadCurrentDeep(Cursor cursor, boolean lock) {
        TransactionPattern entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Account sourceAccount = loadCurrentOther(daoSession.getAccountDao(), cursor, offset);
         if(sourceAccount != null) {
            entity.setSourceAccount(sourceAccount);
        }
        offset += daoSession.getAccountDao().getAllColumns().length;

        Category category = loadCurrentOther(daoSession.getCategoryDao(), cursor, offset);
        entity.setCategory(category);
        offset += daoSession.getCategoryDao().getAllColumns().length;

        Subcategory subcategory = loadCurrentOther(daoSession.getSubcategoryDao(), cursor, offset);
        entity.setSubcategory(subcategory);
        offset += daoSession.getSubcategoryDao().getAllColumns().length;

        Account destAccount = loadCurrentOther(daoSession.getAccountDao(), cursor, offset);
        entity.setDestAccount(destAccount);

        return entity;    
    }

    public TransactionPattern loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<TransactionPattern> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<TransactionPattern> list = new ArrayList<TransactionPattern>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<TransactionPattern> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<TransactionPattern> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
